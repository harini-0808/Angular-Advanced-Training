import { ForbiddenPidDirective } from './forbidden-pid.directive';

describe('ForbiddenPidDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenPidDirective();
    expect(directive).toBeTruthy();
  });
});
